name: "CD — Development"

on:
  push:
    branches: ['develop']
  workflow_dispatch:

concurrency:
  group: cd-development
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: CI checks
        run: |
          npm run lint
          npm run typecheck
          npm test
      - name: Build
        run: npm run build
        env:
          NODE_ENV: production
          NEXT_PUBLIC_ENVIRONMENT: development
          NEXT_PUBLIC_APP_URL: ${{ secrets.APP_URL_DEV }}
          NEXT_PUBLIC_APP_VERSION: ${{ github.sha }}
      - name: Package build
        run: tar --ignore-failed-read -czf build-dev.tar.gz .next/ public/ package.json package-lock.json next.config.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: build-dev-${{ github.sha }}
          path: build-dev.tar.gz
          retention-days: 3

  deploy-development:
    name: Deploy to Development
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build-dev-${{ github.sha }}
      - run: tar -xzf build-dev.tar.gz
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.HOSTINGER_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.HOSTINGER_PORT }} -H ${{ secrets.HOSTINGER_HOST }} >> ~/.ssh/known_hosts 2>/dev/null
      - name: Deploy via rsync
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/deploy_key -p ${{ secrets.HOSTINGER_PORT }} -o StrictHostKeyChecking=no" \
            .next/ public/ package.json package-lock.json next.config.mjs \
            ${{ secrets.HOSTINGER_USERNAME }}@${{ secrets.HOSTINGER_HOST }}:${{ secrets.HOSTINGER_DEPLOY_PATH_DEV }}/
      - name: Install & restart
        run: |
          ssh -i ~/.ssh/deploy_key -p ${{ secrets.HOSTINGER_PORT }} -o StrictHostKeyChecking=no \
            ${{ secrets.HOSTINGER_USERNAME }}@${{ secrets.HOSTINGER_HOST }} \
            "cd ${{ secrets.HOSTINGER_DEPLOY_PATH_DEV }} && npm install --production && pm2 restart tv-sales-dev || pm2 start npm --name tv-sales-dev -- start"
      - name: Health check
        run: |
          sleep 10
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" ${{ secrets.APP_URL_DEV }}/api/health)
          if [ "$STATUS" != "200" ]; then echo "Health check failed - HTTP $STATUS" && exit 1; fi
          echo "Health check passed - HTTP $STATUS"
      - name: Summary
        run: |
          echo "## Development Deployment Successful" >> $GITHUB_STEP_SUMMARY
          echo "Commit: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "URL: ${{ secrets.APP_URL_DEV }}" >> $GITHUB_STEP_SUMMARY
